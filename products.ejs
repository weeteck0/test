<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>SL9000</title>
</head>
<style>
  body {
    font-family: sans-serif;
    background: linear-gradient(to right, hsl(210, 62%, 85%), #86adf5);
}
</style>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">SL9000</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/products">Browse Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/addProduct">Add Product</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <% if (user) { %>
            <h3>Hello, <%= user.username %> (<%= user.role %>)</h3>
        <% } %>
        <br>
        
        <!-- Simple search and filter form -->
        <div class="row mb-3">
            <div class="col-md-8">
                <div class="text-center"><h2>PC Components Catalog</h2></div>
            </div>
            <div class="col-md-4">
                <!-- search and filter products -->
                <form method="GET" action="/products">
                    <div class="mb-2">
                        <input type="text" class="form-control" name="search" placeholder="Search products..." 
                               value="<%= (filters && filters.search) ? filters.search : '' %>">
                    </div>
                    <div class="mb-2">
                        <select class="form-control" name="category">
                            <option value="">All Categories</option>
                            <% if (categories && categories.length > 0) { %>
                                <% for(let i=0; i < categories.length; i++) { %>
                                    <option value="<%= categories[i].category %>" 
                                            <%= (filters && filters.category === categories[i].category) ? 'selected' : '' %>>
                                        <%= categories[i].category %>
                                    </option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                    <button class="btn btn-primary w-100" type="submit">Filter</button>
                </form>
            </div>
        </div>
        
        <!-- Card layout -->
        <div class="row">
            <% if (!products || products.length === 0) { %>
                <div class="col-12">
                    <p class="text-center">No products found. Try different search terms.</p>
                </div>
            <% } else { %>
                <% for(let i=0; i < products.length; i++) { %>
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="card">
                            <!-- Category badge -->
                            <div class="card-header">
                                <% if (products[i].category) { %>
                                    <span class="badge bg-primary"><%= products[i].category %></span>
                                <% } %>
                                <% if (products[i].brand) { %>
                                    <span class="badge bg-secondary"><%= products[i].brand %></span>
                                <% } %>
                            </div>
                            
                            <img src="/images/<%= products[i].image_url || 'no-image.jpg' %>" 
                                 class="card-img-top" style="height: 200px; object-fit: cover;" 
                                 alt="<%= products[i].name || 'Product Image' %>">
                            
                            <div class="card-body">
                                <!-- Product name as clickable link -->
                                <h5 class="card-title">
                                    <a href="/product/<%= products[i].id %>"><%= products[i].name || 'Unnamed Product' %></a>
                                </h5>
                                
                                <!-- Product description -->
                                <% if (products[i].description) { %>
                                    <p class="card-text small"><%= products[i].description.substring(0, 60) %>...</p>
                                <% } %>
                                
                                <!-- Product prices with proper formatting -->
                                <% if (products[i].price !== undefined && products[i].price !== null) { %>
                                    <p class="h5 text-primary">$<%= parseFloat(products[i].price).toFixed(2) %></p>
                                <% } else { %>
                                    <p class="h5 text-muted">Price not available</p>
                                <% } %>
                                
                                <!-- Stock status -->
                                <% if (products[i].stock_quantity !== undefined && products[i].stock_quantity !== null) { %>
                                    <% if (products[i].stock_quantity > 0) { %>
                                        <p class="text-success small">✓ <%= products[i].stock_quantity %> in stock</p>
                                    <% } else { %>
                                        <p class="text-danger small">✗ Out of stock</p>
                                    <% } %>
                                <% } %>
                                
                                <!-- Action buttons -->
                                <div class="d-grid gap-2">
                                    <a href="/product/<%= products[i].id %>" class="btn btn-outline-primary">
                                        View Details
                                    </a>
                                    <a href="/updateProduct/<%= products[i].id %>" class="btn btn-secondary btn-sm">
                                        Edit
                                    </a>
                                    <a href="/deleteProduct/<%= products[i].id %>" 
                                       onclick="return confirm('Are you sure you want to delete this product?')"
                                       class="btn btn-danger btn-sm">
                                        Delete
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>
</body>
</html>
