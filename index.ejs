<!DOCTYPE html>
<html>
<head>
    <title>Sim Lim 9000 - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
  body {
    font-family: 'Times New Roman', Times, serif;
    background-color: rgb(184, 223, 255);
      .jumbotron a:hover {
    color: rgb(136, 67, 220);
    text-decoration: underline;
  }
  }
</style>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">SL9000</a>
            <div class="navbar-nav ms-auto">
                <% if (!user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li> 
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li> 
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/products">Products</a>
                    </li>
                    <% if (user.role === 'admin') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/addProduct">Add Product</a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li> 
                <% } %>
            </div>
        </div>
    </nav>

</div>
<div class="container-fluid px-0">
  <div id="homepageCarousel" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
      
      <div class="carousel-item active">
        <img src="/images/pc1.jpg" alt="pc1" style="width: 100%; height: 250px; object-fit: cover;">
        <div class="carousel-caption d-none d-md-block">
          <h1 class="mt-5">Welcome to Sim Lim 9000</h1>
        </div>
      </div>

      <div class="carousel-item">
        <img src="/images/pc2.jpg" alt="pc2" style="width: 100%; height: 250px; object-fit: cover;">
        <div class="carousel-caption d-none d-md-block">
          <h1 class="mt-5">Welcome to Sim Lim 9000</h1>
        </div>
      </div>

      <div class="carousel-item">
        <img src="/images/pc3.jpg" alt="pc3" style="width: 100%; height: 250px; object-fit: cover;">
        <div class="carousel-caption d-none d-md-block">
          <h1 class="mt-5">Welcome to Sim Lim 9000</h1>
        </div>
      </div>
    </div>
  </div>
</div>
        
        <% if (messages && messages.length > 0) { %>
            <div class="alert alert-success">
                <% messages.forEach(function(message) { %>
                    <p><%= message %></p>
                <% }); %>
            </div>
        <% } %>
        
        <% if (user) { %>
            <div class="alert alert-info">
                <p>Hello, <%= user.username %> (<%= user.role %>)!</p>
                <% if (user.role === "admin") { %>
                    <p><a href="/products" class="btn btn-primary">Manage Products</a></p>
                <% } else { %>
                    <p><a href="/products" class="btn btn-primary">Browse Products</a></p>
                <% } %>
            </div>
        <% } else { %>
            <div class="jumbotron">
                <h3>This Is Where You Build Your Dream PC.</h3> 
                <p>Please <a href="/login">login</a> to continue or <a href="/register">register</a> for a new account.</p>
            </div>
        <% } %>
    </div>
</body>
</html>
